{
	"version": 2,
	"builds": [
			{
					"src": "vercel-build.sh",
					"use": "@vercel/static-build",
					"config": {
							"distDir": ".markpub/output"
					}
			}
	],
	"routes": [
			{ "src": "/(.*)", "dest": "/$1" }
	]
}
