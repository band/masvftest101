{
  "version": 2,
  "framework": null,
  "buildCommand": "python -m venv venv && . venv/bin/activate && pip install --upgrade pip && pip install -r .nxc/requirements.txt && cd .nxc && nxc build -i .. -o ../public --lunr --commits",
  "outputDirectory": "public",
  "builds": [
    {
      "src": "**",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/public/$1"
    }
  ],
  "env": {
    "PYTHON_VERSION": "3.8"
  }
}
