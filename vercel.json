{
  "version": 2,
  "buildCommand": "cd .nxc && npm install && python -m pip install --upgrade pip && pip install -r requirements.txt && nxc -i .. -o ./output --lunr --commits",
  "outputDirectory": ".nxc/output",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ],
  "env": {
    "PYTHON_VERSION": "3.8",
    "NODE_VERSION": "16"
  }
}
