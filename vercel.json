{
  "version": 2,
  "framework": null,
  "buildCommand": "cd .nxc && nxc build -i .. -o ../public -r /ennextest --commits",
  "outputDirectory": "public",
  "builds": [
    {
      "src": "**",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/public/$1"
    }
  ],
  "env": {
    "PYTHON_VERSION": "3.8"
  },
  "installCommand": "python -m venv venv && . venv/bin/activate && pip install --upgrade pip && pip install -r .nxc/requirements.txt",
  "public": true
}
